{
    "name": "my-world",
    "type": "module",
    "scripts": {
        "build": "vite build",
        "dev": "vite",
        "preview": "vite preview",
        "zip": "npm run build && mkdir -p dist && zip -r dist/game.zip dist -x \"**/.DS_Store\"",
        "check": "tsc"
    },
    "dependencies": {
        "kaplay": "^3001.0.10"
    },
    "devDependencies": {
        "@types/node": "^22.14.0",
        "typescript": "^5.8.3",
        "vite": "^6.2.5"
    }
}
